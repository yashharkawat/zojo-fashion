[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.build]
cmds = ["cd apps/api && npm install --include=dev && npx prisma generate --schema=../../prisma/schema.prisma && npm run build"]

[start]
cmd = "cd apps/api && node dist/server.js"
