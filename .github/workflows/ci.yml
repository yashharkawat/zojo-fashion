name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '22'

jobs:
  # ─── Backend checks ───────────────────────────────────────
  api-check:
    name: API — typecheck + lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: apps/api/package-lock.json

      - run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate --schema=../../prisma/schema.prisma

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint --if-present

  # ─── Frontend checks ──────────────────────────────────────
  web-check:
    name: Web — typecheck + lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: apps/web/package-lock.json

      - run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint --if-present

  # ─── Prisma schema validation ─────────────────────────────
  schema-check:
    name: Prisma schema validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - run: npm i -g prisma

      - name: Validate schema
        run: prisma validate --schema=prisma/schema.prisma

      - name: Check for pending migrations
        run: prisma migrate status --schema=prisma/schema.prisma
        env:
          DATABASE_URL: 'postgresql://dummy:dummy@localhost:5432/dummy'
        # This will warn about unapplied migrations but won't fail the job
        # unless the schema itself is invalid.
        continue-on-error: true
